#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"

echo "Building the app..."
npm run build

echo "Starting production server (http://0.0.0.0:3000)..."
echo "Access from other devices: http://$(ipconfig getifaddr en0 2>/dev/null || hostname):3000"
npm run start -- -H 0.0.0.0 -p 3000
